#include <bits/stdc++.h>
using namespace std;

void solve() {
    int first, second;
    cin >> first >> second;

    int res = first * second + 1;

    cout << res << endl;


}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int ts;
    cin >> ts;

    while (ts--)
    {
        solve();
    }
    
    
    return 0;
}